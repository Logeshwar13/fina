import { apiFetch } from '../utils.js';
import { authState } from '../auth.js';

/**
 * AI Assistant Component - DISABLED
 * 
 * This floating chat widget has been disabled in favor of the new AI Chat page.
 * The function is kept for compatibility but does nothing.
 * 
 * To use AI chat, navigate to "AI Chat" in the sidebar.
 */

export function initAIAssistant() {
    console.log('Old AI Assistant disabled - use AI Chat page from sidebar');
    // Function intentionally does nothing - AI Chat page is the new interface
}
